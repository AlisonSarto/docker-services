services:
  appsmith:
    image: appsmith/appsmith-ce:latest
    container_name: appsmith

    environment:
      - APPSMITH_BASE_URL=${BASE_URL}

    ports:
      - "8000:80"

    volumes:
      - ./stacks:/appsmith-stacks
         
    restart: unless-stopped